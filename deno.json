{
  "name": "@synitio/kv-magic-link-auth",
  "version": "0.1.1",
  "description": "Deno KV backed magic-link authentication for server-side Deno apps.",
  "license": "MIT",
  "exports": {
    ".": "./mod.ts"
  },
  "tasks": {
    "check": "deno fmt --check && deno lint && deno test --unstable-kv --allow-read --allow-write",
    "test": "deno test --unstable-kv --allow-read --allow-write"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.15"
  },
  "fmt": {
    "semiColons": true,
    "singleQuote": false
  },
  "publish": {
    "exclude": [
      ".gitignore",
      "CHANGELOG.md",
      "mod_test.ts"
    ]
  }
}
